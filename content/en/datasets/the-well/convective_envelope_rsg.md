---
title: "Convective Envelope of a Red Supergiant"
parent_collection: "The Well"
physical_family: "Radiation hydrodynamics"
spatial_dimension: 3D
coordinate_system: "spherical-polar $(\\\\log r,\\\\theta,\\\\phi)$"
storage_format: "HDF5 / The Well unified schema / fp32 release arrays"
dataset_license: CC-BY-4.0
code_license: BSD-3-Clause
official_page: "https://polymathic-ai.org/the_well/datasets/convective_envelope_rsg/"
repository_directory: "https://github.com/PolymathicAI/the_well/tree/master/datasets/convective_envelope_rsg"
huggingface_dataset: "https://huggingface.co/datasets/polymathic-ai/convective_envelope_rsg"
paper: "https://arxiv.org/abs/2412.00568"
status: active
repository_release_checked: "v1.2.0 / master"
last_verified: 2026-07-21
linkTitle: convective_envelope_rsg
weight: 70
draft: false
ShowToc: true
TocOpen: true
hidemeta: true
ShowBreadCrumbs: true
ShowPostNavLinks: true
math: true
hiddenInHomeList: true
dataset_family: "The Well"
summary: "Three-dimensional radiation hydrodynamics models the turbulent, convective envelope of a red supergiant. The released trajectories are temporal cuts from one much longer global…"
description: "Three-dimensional radiation hydrodynamics models the turbulent, convective envelope of a red supergiant. The released trajectories are temporal cuts from one much longer global…"

---

# Convective Envelope of a Red Supergiant

> **Parent collection:** The Well  
> **Directory:** `convective_envelope_rsg`  
> **Equation family:** Radiation hydrodynamics  
> **Documentation type:** source-faithful structured rewrite of the official dataset page, paper appendix and current repository metadata.

## 1. Scope and physical overview

Three-dimensional radiation hydrodynamics models the turbulent, convective envelope of a red supergiant. The released trajectories are temporal cuts from one much longer global stellar simulation rather than independent parameter sweeps.

The Well treats each downloadable directory as a self-documenting HDF5 dataset. This page separates three notions that are often conflated: parameters that are theoretically adjustable in the equations/generator, parameters actually varied in the released ensemble, and parameters fixed in this release.

## 2. Governing equations

$$
\frac{\partial\rho}{\partial t}+\nabla\cdot(\rho\mathbf v)=0,
$$
$$
\frac{\partial(\rho\mathbf v)}{\partial t}
+\nabla\cdot(\rho\mathbf v\mathbf v+\mathbf P_{\rm gas})
=-\mathbf G_r-\rho\nabla\Phi,
$$
$$
\frac{\partial E}{\partial t}
+\nabla\cdot[(E+P_{\rm gas})\mathbf v]
=-cG_r^0-\rho\mathbf v\cdot\nabla\Phi,
$$
$$
\frac{\partial I}{\partial t}+c\,\mathbf n\cdot\nabla I=S(I,\mathbf n).
$$
The comoving radiation source uses Planck/Rosseland absorption opacities and electron-scattering opacity as documented in the paper.

### Variables and physical fields

- \(
ho,\mathbf v,P_{
m gas},E\): gas density, velocity, pressure and total gas energy density.
- \(I(t,\mathbf x,\mathbf n)\): frequency-integrated radiation intensity.
- \(G_r^0,\mathbf G_r\): radiation four-force components.
- \(\Phi\): gravitational potential generated by enclosed mass.
- \(\kappa_{aP},\kappa_{aR},\kappa_S\): opacity coefficients.

## 3. Parameter audit

| Category | Released-data interpretation |
|---|---|
| Theoretically adjustable | Stellar mass/structure, gravity profile, opacity tables, luminosity, radial/angular extent, rotation, magnetic field, initial perturbations, spatial resolution and sampling cadence. |
| Actually varied in this release | No physical coefficient is intentionally swept across the 29 released trajectories. They correspond to different time intervals/cuts of the same long simulation, so the varying factors are turbulent state and temporal phase rather than stellar parameters. |
| Fixed in this release | Stellar model, gravity/opacity setup, numerical resolution, angular wedge, neglect of rotation and magnetic fields, and the generating simulation's physical parameters. |

## 4. Initial and boundary conditions

### Initial conditions

The 29 trajectories begin from states sampled from one developed global simulation; they are not independent random initial conditions.

### Boundary conditions

Spherical-polar wedge with periodic/ghost-zone angular treatment as described by Athena++; radial boundaries are those of the original stellar-envelope setup.

## 5. Data specification

| Property | Value |
|---|---|
| Parent collection | The Well |
| Download directory | `convective_envelope_rsg` |
| Spatial dimension | 3D |
| Coordinate system | spherical-polar $(\log r,\theta,\phi)$ |
| Spatial resolution | $256\times128\times256$ |
| Stored steps per trajectory | 100 |
| Number of trajectories | 29 temporal cuts |
| Dynamic channels after component expansion | 6 |
| Dynamic fields | energy, density, pressure, velocity $(v_r,v_\theta,v_\phi)$ |
| Static fields / scalar context | grid geometry and common stellar/simulation parameters |
| Time range | dataset-specific physical times |
| Stored time spacing | uniform within released trajectories |
| Spatial domain | global spherical-polar wedge |
| Estimated release size | 570 GB |
| Storage | HDF5, shared The Well schema, released arrays in fp32 |
| Default split convention | Usually 80/10/10 over trajectories/initial conditions; small-trajectory datasets may use blocked time segments. Inspect the downloaded metadata. |

### Raw and model-facing shapes

The raw HDF5 schema stores scalar, vector and tensor fields separately. A typical dynamic scalar field has conceptual shape

\[
(N_{\rm traj},N_t,L_1,L_2[,L_3]),
\]

while vector/tensor fields append their component axes. `WellDataset` can flatten physical components into a final channel axis. With batch size \(B\), this directory is represented conceptually as:

- full sequence: \((B,T,L_1,L_2,L_3,6)\);
- standard 4-frame history: \((B,4,L_1,L_2,L_3,6)\);
- standard 1-frame target: \((B,1,L_1,L_2,L_3,6)\).

The order of fields/components should be read from the HDF5 metadata rather than inferred from this conceptual list. Time-independent geometry/coefficient fields are normally model inputs but not prediction targets.

## 6. Numerical generation

**Documented solver:** Athena++; Godunov radiation-hydrodynamics solver

Athena++ solves the RHD system with a Godunov method on a spherical-polar grid. The released radial direction is logarithmic. Angular boundaries use the simulation's periodic/ghost-zone treatment; the source simulation neglects stellar rotation and magnetic fields. Generation required approximately two months on 80 NASA Pleiades Skylake nodes (reported aggregate setup).

## 7. Recommended ML tasks and diagnostics

Forecasting multi-scale stellar convection, steady/statistical-state prediction, transfer across temporal cuts, reduced modeling of radiation hydrodynamics and simulation acceleration.

Useful evaluation should go beyond aggregate RMSE when possible: report per-field errors, long-rollout stability, conservation or balance diagnostics, and spectral/scale-resolved error for turbulent or wave systems.

## 8. Version reconciliation and cautions

Treat trajectory count as temporal segmentation, not parameter diversity. The source description mentions a larger native radial grid (384/256 zones depending on region/setup), whereas the released ML tensor is \(256\times128\times256\); use HDF5 coordinates for exact array geometry.

Current repository status: **active**. This document was checked against the repository state/release information available on 2026-07-21. Always inspect `dataset_name.yaml`, `stats.yaml`, HDF5 coordinates and release notes for the exact files used in an experiment.

## Download and loading

### Install the interface

```bash
python -m venv .venv
source .venv/bin/activate
pip install the_well
```

### Download one split

```bash
the-well-download --base-path ./the_well_data --dataset convective_envelope_rsg --split train
```

Download all standard splits:

```bash
for split in train valid test; do
  the-well-download --base-path ./the_well_data --dataset convective_envelope_rsg --split "$split"
done
```

Omitting both `--dataset` and `--split` requests the full collection and should not be done accidentally because The Well is about 15 TB.

### Load local data

```python
from the_well.data import WellDataset
from torch.utils.data import DataLoader

trainset = WellDataset(
    well_base_path="./the_well_data",
    well_dataset_name="convective_envelope_rsg",
    well_split_name="train",
)
loader = DataLoader(trainset, batch_size=1, shuffle=True)

sample = trainset[0]
print(sample.keys())
```

### Hugging Face streaming

This directory is listed for Hub access in the current collection:

```python
from the_well.data import WellDataset

trainset = WellDataset(
    well_base_path="hf://datasets/polymathic-ai/",
    well_dataset_name="convective_envelope_rsg",
    well_split_name="train",
)
```

For large training runs, local download is normally faster and more reproducible than network streaming.

### Other distribution route

The paper also describes direct Flatiron-hosted distribution and a Globus endpoint. Endpoint details can change, so use the current repository/download documentation rather than hard-coding an old endpoint.


## 9. Links

| Resource | URL |
|---|---|
| Official dataset page | <https://polymathic-ai.org/the_well/datasets/convective_envelope_rsg/> |
| Repository directory | <https://github.com/PolymathicAI/the_well/tree/master/datasets/convective_envelope_rsg> |
| Hugging Face dataset | <https://huggingface.co/datasets/polymathic-ai/convective_envelope_rsg> |
| The Well repository | <https://github.com/PolymathicAI/the_well> |
| Paper | <https://arxiv.org/abs/2412.00568> |
| Data-format documentation | <https://polymathic-ai.org/the_well/data_format/> |
| Hugging Face collection | <https://huggingface.co/collections/polymathic-ai/the-well> |

## 10. Citation and provenance

Recommended citation: Goldberg, Jiang and Bildsten, *Numerical Simulations of Convective Three-dimensional Red Supergiant Envelopes* (2022).

Also cite the collection paper:

> Ohana et al., **The Well: a Large-Scale Collection of Diverse Physics Simulations for Machine Learning**, NeurIPS 2024 Datasets and Benchmarks.

This English page is a structured, source-faithful synthesis, not a byte-for-byte mirror of the website. Equations and numerical values are reconciled from the official dataset documentation, the paper appendix and current repository metadata. The paired Chinese document is an annotated translation and reorganization.
